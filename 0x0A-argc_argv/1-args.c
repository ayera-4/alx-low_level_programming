#include "main.h"
#include <stdio.h>

/**
 * main - print number of args
 * @argc: number of input cmd parameters
 * @argv: array of input cmd strings
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc);
return (0);
}
