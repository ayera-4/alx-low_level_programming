#include "main.h"

/**
 * mul - multiplies two integers
 * @a: input parameter
 * @b: input parameter
 * Return: Always 0
 */
int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
