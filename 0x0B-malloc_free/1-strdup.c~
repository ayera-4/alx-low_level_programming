#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - returns pointer to a newly allocated space
 * @str: input parameter
 * Return: pointer
 */
char *_strdup(char *str)
{
  char *s;
  s = malloc(sizeof(char) * s);
}
